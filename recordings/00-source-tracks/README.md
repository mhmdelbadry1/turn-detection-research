# Source tracks

`human.ogg` = user (VAP channel 0). `agent.ogg` = TTS / agent (VAP channel 1).

Session `656e9b45-5eeb-4fce-9295-d919fe212464`. Align metadata: `audio-align.json`.

These are the only files in this package that VAP should ingest. The `*-after.wav` folders are listen mixes, not model input.

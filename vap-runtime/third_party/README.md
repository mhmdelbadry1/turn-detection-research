KTH Voice Activity Projection Python package (copied from `vap-src/vap` only — no Switchboard dumps).

Upstream: https://github.com/ErikEkstedt/VoiceActivityProjection

Put this directory on `PYTHONPATH` (the example does `PYTHONPATH=third_party:.`).
